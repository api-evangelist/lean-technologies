{
  "opencollection": "1.0.0",
  "info": {
    "name": "Consents Account On File Account Controls (New) Account Controls (New) Account Controls (New) API",
    "version": "v0.2.3"
  },
  "request": {
    "auth": {
      "type": "bearer",
      "token": "{{bearerToken}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "Account Controls (New)",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Get Account Controls",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://sandbox.leantech.me/insights/v3/account-controls",
            "params": [
              {
                "name": "entity_id",
                "value": "",
                "type": "query",
                "description": "The `entity_id` you're querying for."
              }
            ]
          },
          "docs": "Offers overview signals and configurable thresholds on a connected account,  returning a simple eligible/not-eligible verdict before any downstream process begins"
        }
      ]
    }
  ],
  "bundled": true
}